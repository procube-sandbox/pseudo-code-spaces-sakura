# 環境変数設定ファイル（サンプル）
# 
# ローカル環境またはDev Containerで使用する場合:
#   1. このファイルを .env にコピー
#   2. 値を実際の値に置き換え
#   3. source .env で読み込む
#
# GitHub CodeSpacesで使用する場合:
#   - リポジトリの Settings → Secrets and variables → Codespaces でSecretsを設定
#   - Secretsは自動的に大文字に変換されるため、以下の大文字版で登録:
#     TF_VAR_SAKURA_TOKEN, TF_VAR_SAKURA_SECRET, 等
#   - .devcontainer/devcontainer.json が自動的に小文字混じりに変換します

# Sakura Cloud API Credentials
# さくらのクラウドのコントロールパネルから取得してください
# https://secure.sakura.ad.jp/cloud/
export TF_VAR_sakura_token="your-sakura-cloud-token"
export TF_VAR_sakura_secret="your-sakura-cloud-secret"

# さくらのクラウドのゾーン (is1a, is1b, tk1a, tk1v など)
export TF_VAR_zone="is1a"

# SSH Public Key
# ~/.ssh/id_rsa.pub の内容をコピーしてください
# 例: ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQ...
export TF_VAR_ssh_public_key="ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQ..."

# Domain Configuration
# 事前に取得したドメイン名を設定してください
export TF_VAR_domain="your-domain.com"

# GitHub OAuth App Credentials
# https://github.com/settings/developers でOAuth Appを作成してください
# Callback URL: https://your-domain.com/auth/github/callback
export TF_VAR_github_client_id="your-github-oauth-client-id"
export TF_VAR_github_client_secret="your-github-oauth-client-secret"

# Server Configuration (オプション)
# デフォルト値を変更する場合は以下のコメントを外してください
# export TF_VAR_server_name="workspaces"
# export TF_VAR_server_core="4"
# export TF_VAR_server_memory="8"
# export TF_VAR_disk_size="100"

# Ansible用の環境変数
export DOMAIN="$TF_VAR_domain"
export GITHUB_CLIENT_ID="$TF_VAR_github_client_id"
export GITHUB_CLIENT_SECRET="$TF_VAR_github_client_secret"

# Logging Configuration (オプション)
# ログレベル: trace, debug, info, warn, error, fatal
# デフォルト: info
# export LOG_LEVEL="info"
